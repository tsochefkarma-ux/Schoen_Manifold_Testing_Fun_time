# Supplementary SageMath material

This directory accompanies **Modular Fixed-Point Rank Reduction and Degree-Two Obstructions in an HSS-Motivated Schoen Sector**.

## Logical status

- `S1`-`S5` use exact rational-function arithmetic, apart from the explicitly marked Arb/real-ball sign and root enclosures.
- `S6`-`S7` reconstruct the rational-elliptic-surface data and perform the exact one-sided correction-kernel test.
- `S8`-`S9` implement the natural factorized order-`p^2` Schoen ansatz. Their final inconsistency result is a high-precision numerical experiment, not an exact theorem about the full relative-contact degree-two potential.

## Suggested order

1. `S1_fixed_point_defect_membership.sage`
2. `S2_local_closure_locus_rigidity.sage`
3. `S3_modular_path_transversality.sage`
4. `S4_linearized_correction_jet_test.sage`
5. `S5_extract_correction_kernel_structure.sage` in the same session as S4
6. `S6_WDVV_M2_reconstruction.sage`
7. `S7_HSS_gamma_degree2_gate.sage`; its optional kernel adapter requires S4 data in the same session
8. `S8_factorized_gluing_full_p2.sage`
9. If S8 stops at the tiny-determinant guard after building `DATA`, run `S9_rank_revealing_continuation.sage` in the same session.

## High-precision confirmation

The included `S9_high_precision_output.txt` records the user-run confirmation with:

- 1280-bit real arithmetic;
- `q`-series through `q^2000`;
- thirteen radial sample points;
- stable ranks `rank(A)=4`, `rank([A|b])=5` for pivot tolerances from `1e-20` to `1e-345`;
- order-one residuals for the three named factorized candidates.

## Scope warning

The span generated by `F_A`, `F_B`, and `F_C` is a natural factorized ansatz built from exact one-sided rational-surface data. It is not asserted to equal the complete degree-two Schoen relative-contact potential.
